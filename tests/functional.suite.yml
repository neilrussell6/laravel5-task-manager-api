# Codeception Test Suite Configuration
#
# Suite for functional (integration) tests
# Emulate web requests and make application process them
# Include one of framework modules (Symfony2, Yii2, Laravel5) to use it

class_name: FunctionalTester
modules:
    enabled:
        - \Helper\Functional
        - Filesystem
        - Asserts
        - Laravel5:
            environment_file: .env-testing
            cleanup: true # run tests in transaction mode
#        - Db:
#            dsn: 'mysql:host=localhost;dbname=tt_task_list_testing'
#            user: 'homestead'
#            password: 'secret'
#            dump: 'tests/_data/functional.sql'
#            populate: true # whether the the dump should be loaded before the test suite is started
#            cleanup: false # whether the dump should be reloaded after each test
#            reconnect: false # whether the module should reconnect to the database before each test